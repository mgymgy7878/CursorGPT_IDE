import SupervisorAgent from "./SupervisorAgent"

// Uygulama genelinde tek bir SupervisorAgent örneği kullanmak için singleton
const supervisorAgent = new SupervisorAgent()

export default supervisorAgent 