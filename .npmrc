# pnpm configuration
# Ref: https://pnpm.io/npmrc

# CI ve local tutarlılık: lockfile değişikliklerini engelle
prefer-frozen-lockfile=true

# Peer dependency'leri sıkı kontrol et
strict-peer-dependencies=true

# Hoist pattern (monorepo için)
hoist=true
hoist-pattern[]=*eslint*
hoist-pattern[]=*prettier*
hoist-pattern[]=*typescript*

# Store location (performans)
store-dir=~/.pnpm-store

# Progress reporting (CI'da gürültü azaltma)
reporter=silent

# Network timeout
fetch-timeout=60000

# Registry (varsayılan npm registry)
registry=https://registry.npmjs.org/

