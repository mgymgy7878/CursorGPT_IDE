name: Lighthouse CI (Canary)

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/web-next/**"
      - ".github/workflows/lighthouse.yml"
  workflow_dispatch:

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    continue-on-error: true # Canary mode

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20.11.1"

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.18.3

      - name: Install dependencies
        run: |
          set -euxo pipefail
          pnpm -w install --frozen-lockfile || pnpm -w install
          pnpm --filter web-next install || true

      - name: Build web-next
        run: pnpm --filter web-next build
        env:
          NODE_ENV: production
          CI_CANARY: '1'

      - name: Start web-next (next start)
        working-directory: apps/web-next
        env:
          NODE_ENV: production
          PORT: 3003
          HOSTNAME: 127.0.0.1
          CI_CANARY: '1'
        run: |
          set -euo pipefail
          nohup npx next start -p 3003 -H 127.0.0.1 > ../../canary-server.log 2>&1 &

      - name: Wait for health (120s)
        run: |
          set -euo pipefail
          for i in {1..24}; do
            if curl -fsS http://127.0.0.1:3003/api/health >/dev/null 2>&1; then
              echo "✅ Server ready on attempt $i"
              exit 0
            fi
            sleep 5
          done
          echo "❌ Health endpoint timeout" >&2
          exit 1

      - name: Install Lighthouse CI
        run: npm install -g @lhci/cli@0.13.x

      - name: Run Lighthouse
        run: |
          lhci autorun \
            --collect.url=http://127.0.0.1:3003 \
            --upload.target=temporary-public-storage || echo "⚠️ Lighthouse warnings (canary mode)"

      - name: Upload Lighthouse results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-results
          path: |
            .lighthouseci/**/*
            canary-server.log
          retention-days: 7

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-debug
          path: |
            canary-server.log
            apps/web-next/.next/**
          retention-days: 1
